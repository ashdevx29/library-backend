import express from 'express';import{protect,authorize}from'../middleware/authMiddleware.js';import{validateDashboardQuery as valid}from'../middleware/DashboardValidation.js';import * as d from'../controllers/DashboardController.js';const r=express.Router();r.use(protect,valid);const admin=authorize('Super Admin','Branch Admin','Staff');[['summary',d.adminSummary],['attendance',d.adminAttendance],['revenue',d.adminRevenue],['expenses',d.adminExpenses],['occupancy',d.adminOccupancy],['activity',d.adminActivity],['expiring-members',d.adminExpiringMembers],['growth',d.adminGrowth]].forEach(([p,h])=>r.get('/admin/'+p,admin,h));[['summary',d.userSummary],['attendance',d.userAttendance],['membership',d.userMembership],['notifications',d.userNotifications]].forEach(([p,h])=>r.get('/user/'+p,authorize('Student'),h));export default r;